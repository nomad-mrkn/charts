
{{- if .Values.kubeScheduler.enabled }}
{{- if .Values.kubeScheduler.endpoints }}
---
apiVersion: v1
kind: Endpoints
metadata:
  name: {{ printf "%s-%s" (include "vm-operator.fullname" $) "kube-scheduler" | trunc 63 | trimSuffix "-" }}
  namespace: kube-system
  labels:
    app: {{ include "vm-operator.fullname" . }}-kube-scheduler
    k8s-app: kube-scheduler
{{ include "vm-operator.labels" . | indent 4 }}
subsets:
  - addresses:
      {{- range .Values.kubeScheduler.endpoints }}
      - ip: {{ . }}
      {{- end }}
    ports:
      - name: http-metrics
        port: {{ .Values.kubeScheduler.service.port }}
        protocol: TCP
{{- end }}
{{- if .Values.kubeScheduler.service.enabled }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ printf "%s-%s" (include "vm-operator.fullname" $) "kube-scheduler" | trunc 63 | trimSuffix "-" }}
  namespace: kube-system
  labels:
    app: {{ include "vm-operator.fullname" . }}-kube-scheduler
    k8s-app: kube-scheduler
{{ include "vm-operator.labels" . | indent 4 }}
spec:
  clusterIP: None
  ports:
    - name: http-metrics
      port: {{ .Values.kubeScheduler.service.port }}
      protocol: TCP
      targetPort: {{ .Values.kubeScheduler.service.targetPort }}
{{- if .Values.kubeScheduler.endpoints }}{{- else }}
  selector:
    {{- if .Values.kubeScheduler.service.selector }}
{{ toYaml .Values.kubeScheduler.service.selector | indent 4 }}
    {{- else}}
    component: kube-scheduler
    {{- end}}
{{- end }}
  type: ClusterIP
{{- end }}
---
apiVersion: operator.victoriametrics.com/v1beta1
kind: VMServiceScrape
metadata:
  name: {{ printf "%s-%s" (include "vm-operator.fullname" $) "kube-scheduler" | trunc 63 | trimSuffix "-" }}
  namespace: {{ $.Release.Namespace }}
  labels: {{ include "vm-operator.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      app: {{ include "vm-operator.fullname" . }}-kube-scheduler
      k8s-app: kube-scheduler
  namespaceSelector:
    matchNames:
      - "kube-system"
{{ omit .Values.kubeScheduler.spec "selector" "namespaceSelector" | toYaml | indent 2 }}
{{- end }}
